class Tipo {
  constructor(concepto, monto, fecha, tipo, id) {
    this.concepto = concepto;
    this.monto = monto;
    this.fecha = fecha;
    this.tipo = tipo;
    this.id = id;
  }
}

module.exports = Tipo;
